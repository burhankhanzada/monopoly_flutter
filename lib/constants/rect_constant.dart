import 'dart:ui';

import 'package:monopoly_flutter/utils/offset_util.dart';
import 'package:monopoly_flutter/utils/size_util.dart';

final stepRect0 = Rect.fromLTWH(
  rightOffset - cornerSize,
  bottomOffset - cornerSize,
  cornerSize,
  cornerSize,
);

final stepRect1 = Rect.fromLTWH(
  cornerSize + 8 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect2 = Rect.fromLTWH(
  cornerSize + 7 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect3 = Rect.fromLTWH(
  cornerSize + 6 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect4 = Rect.fromLTWH(
  cornerSize + 5 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect5 = Rect.fromLTWH(
  cornerSize + 4 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect6 = Rect.fromLTWH(
  cornerSize + 3 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect7 = Rect.fromLTWH(
  cornerSize + 2 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect8 = Rect.fromLTWH(
  cornerSize + 1 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect9 = Rect.fromLTWH(
  cornerSize + 0 * nonCornerSize,
  bottomOffset - cornerSize,
  nonCornerSize,
  cornerSize,
);

final stepRect10 = Rect.fromLTWH(
  leftOffset,
  bottomOffset - cornerSize,
  cornerSize,
  cornerSize,
);

final stepRect11 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 8 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect12 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 7 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect13 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 6 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect14 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 5 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect15 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 4 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect16 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 3 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect17 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 2 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect18 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 1 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect19 = Rect.fromLTWH(
  leftOffset,
  cornerSize + 0 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect20 = Rect.fromLTWH(
  leftOffset,
  topOffset,
  cornerSize,
  cornerSize,
);

final stepRect21 = Rect.fromLTWH(
  cornerSize + 0 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect22 = Rect.fromLTWH(
  cornerSize + 1 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect23 = Rect.fromLTWH(
  cornerSize + 2 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect24 = Rect.fromLTWH(
  cornerSize + 3 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect25 = Rect.fromLTWH(
  cornerSize + 4 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect26 = Rect.fromLTWH(
  cornerSize + 5 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect27 = Rect.fromLTWH(
  cornerSize + 6 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect28 = Rect.fromLTWH(
  cornerSize + 7 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect29 = Rect.fromLTWH(
  cornerSize + 8 * nonCornerSize,
  topOffset,
  nonCornerSize,
  cornerSize,
);

final stepRect30 = Rect.fromLTWH(
  rightOffset - cornerSize,
  topOffset,
  cornerSize,
  cornerSize,
);

final stepRect31 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 0 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect32 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 1 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect33 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 2 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect34 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 3 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect35 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 4 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect36 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 5 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect37 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 6 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect38 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 7 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);

final stepRect39 = Rect.fromLTWH(
  rightOffset - cornerSize,
  cornerSize + 8 * nonCornerSize,
  cornerSize,
  nonCornerSize,
);
